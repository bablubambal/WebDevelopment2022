import React from 'react'

const Hoby = ({data,name,age}) => {
    // let {data,name} = props
  return (
    <div>
      i am hobbie {data} and my name is {name} and age is {age}
    </div>
  )
}

export default Hoby
